{
  "name": "mori-kohi-aki-matsuri-2026",
  "version": "1.0.23",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "verify:baseline": "node scripts/verify-baseline.mjs",
    "prestart": "npm run build",
    "start": "node server.mjs"
  },
  "devDependencies": {
    "vite": "8.3.0"
  },
  "engines": {
    "node": ">=20.19"
  }
}
