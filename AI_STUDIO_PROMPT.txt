Import this existing project as-is. First read AI_STUDIO_IMPORT.md and follow it exactly. Do not redesign, rewrite, migrate to React, regenerate assets, or change any visible site content. This is already a complete vanilla HTML/CSS/JS Vite site. Run `npm run verify:baseline`, install dependencies, run `npm run dev`, verify the preview on port 3000, run `npm run build`, then `npm run verify:baseline` again. Make only the smallest infrastructure fix if something prevents the existing project from running, and tell me before changing any site/content file.
